# placeholder — uncomment to enable. observability-crds needs ServerSideApply.
#
# apiVersion: argoproj.io/v1alpha1
# kind: ApplicationSet
# metadata:
#   name: observability
#   namespace: argocd
# spec:
#   goTemplate: true
#   goTemplateOptions: ["missingkey=error"]
#   generators:
#     - matrix:
#         generators:
#           - git:
#               repoURL: https://github.com/Nvision-x/nxdeployment-development-env.git
#               revision: development
#               files:
#                 - path: environment.yaml
#           - git:
#               repoURL: https://github.com/Nvision-x/nxdeployment-integration-env.git
#               revision: development
#               files:
#                 - path: environment.yaml
#           - list:
#               elements:
#                 - { chart: observability-crds, wave: "-2" }
#                 - { chart: observability,      wave: "-1" }
#   template:
#     metadata:
#       name: "{{ .chart }}-{{ .environment.cluster.name }}"
#       annotations:
#         argocd.argoproj.io/sync-wave: "{{ .wave }}"
#       finalizers:
#         - resources-finalizer.argocd.argoproj.io
#     spec:
#       project: nvisionx-development
#       destination:
#         name: "{{ .environment.cluster.name }}"
#         namespace: "{{ .environment.namespace }}"
#       sources:
#         - repoURL: "{{ .registry.url }}"
#           chart: "{{ .chart }}"
#           targetRevision: "{{ .registry.defaultVersion }}"
#           helm:
#             releaseName: "{{ .chart }}"
#             valueFiles:
#               - $envvalues/values.yaml
#               - $envvalues/profiles/eks.yaml
#         - repoURL: "https://github.com/Nvision-x/nxdeployment-{{ .environment.name }}-env.git"
#           targetRevision: development
#           ref: envvalues
#       syncPolicy:
#         automated:
#           prune: true
#           selfHeal: true
#         syncOptions:
#           - CreateNamespace=true
#           - ServerSideApply=true
